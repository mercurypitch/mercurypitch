---
name: GitHub CLI
description: Use gh CLI for issues/PRs, not WebFetch
type: reference
---
Use `gh` CLI for all GitHub operations (issues, PRs, API). It is the project convention (see CLAUDE.md): authenticated, structured JSON output, and works regardless of repo visibility. Never use WebFetch for GitHub.
