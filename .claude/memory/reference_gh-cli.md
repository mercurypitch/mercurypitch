---
name: GitHub CLI
description: Use gh CLI for issues/PRs, not WebFetch (repos are private)
type: reference
---
Use `gh` CLI for all GitHub operations (issues, PRs, API). Repos are private so WebFetch cannot access them.
