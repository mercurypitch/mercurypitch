<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- The theme the activity runs under once it has started. Its window keeps
         the launch ground, so nothing white shows between the launch frame and
         the first paint of the web view on a light-mode device. -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@color/mp_launch_ground</item>
    </style>


    <!-- The launch frame: the app's ground and the brand mark, never the stock
         splash and never a white window (S4, owner decision 8). The two
         SplashScreen items answer every release: Android 12 and later draw
         them natively, and on 10 and 11 Theme.SplashScreen's own window
         background is a compat layer-list of the same two. So this theme must
         NOT set android:windowBackground: that replaced the compat drawable
         with a flat field and the mark went missing on 10 and 11. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/mp_launch_ground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
    </style>
</resources>