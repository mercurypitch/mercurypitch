#!/bin/sh
set -eu

# Keep commits cheap. Formatting, linting, generated docs, and typechecking run
# once per work item before the PR; CI is the authoritative full gate.
if ! git diff --cached --check; then
  echo "[pre-commit] Staged changes contain whitespace errors."
  exit 1
fi
