name: Build & Deploy Verification

on:
  pull_request:
    branches: [main]

jobs:
  build:
    name: Build App
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: App/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: App

      - name: Build app
        run: npm run build
        working-directory: App

      - name: Verify build output
        run: |
          # Verify critical build artifacts exist
          test -f App/dist/index.html || exit 1
          test -f App/dist/assets/*.js || exit 1
          test -f App/dist/assets/*.css || exit 1
          echo "Build artifacts verified"