# ============================================================
# UVR API Server Dependencies
# ============================================================

# Audio processing
# audio-separator[gpu]
#  for AMD GPUs
# >=0.44.2: RoFormer registry models + built-in ensemble (same engine as
# the RunPod GPU handler — keep versions aligned with runpod/requirements.txt)
audio-separator>=0.44.2

# Web framework
fastapi
uvicorn
python-multipart

# Utilities
python-dotenv
aiofiles
