import { JamPanel } from '@/components/jam/JamPanel'

/** Jam tab (TAB_JAM). Thin wrapper around JamPanel. */
export function JamPage() {
  return (
    <div id="jam-panel">
      <JamPanel />
    </div>
  )
}
